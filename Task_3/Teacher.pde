// 3.f. In the Teacher tab, declare the class "Teacher" and add 3 global variables: "name", "age", "isFemale", using appropriate data types for each.
class Teacher {

  // declare variables
  String name;
  int age;
  boolean isFemale;
  
 // 3.g.  In the Teacher class, add a constructor that takes in 3 parameters with the names "tmpName",
 
 Teacher(String tmpName, int tmpAge, boolean tmpIsFemale) {
// 3.h.  Initalicerer the variables created in 3.f with the parameters of the constructor added in 3.g 
   name = tmpName;
    age = tmpAge;
    isFemale = tmpIsFemale;
   
 }
  
}
